# Copy this file to `migration.conf`.
# Don't change any values unless the database files have been moved.
# All paths are relative to `bot/data`.
[Original]
settings_db = config_data.db
session_db = sessions.db

# This can also be None if the session data doesn't need to be transferred
savefile = timerstatus.json

[Target]
database = data.db
savefile = timerstatus/timerstatus.pickle
